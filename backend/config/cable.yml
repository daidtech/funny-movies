development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_CABLE_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: funny_video_development

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_CABLE_URL") { ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } } %>
  channel_prefix: funny_video_production
